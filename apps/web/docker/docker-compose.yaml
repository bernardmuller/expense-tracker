services:
  database:
    image: postgres:14
    restart: always
    environment:
      POSTGRES_DB: expense_tracker
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "6543:5432"



# DATABASE_URL=postgresql://devuser:devpassword@localhost:5420/devdb
